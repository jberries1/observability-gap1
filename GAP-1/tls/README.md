Каталог для TLS-материала и basic-auth шлюза экспортеров.

Содержимое генерится локально и в репозиторий не коммитится:

```
./gen-certs.sh
```

На выходе: `ca.crt`, `ca.key`, `gw.crt`, `gw.key`, `htpasswd`, `gw_password`.
